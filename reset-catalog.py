#!/usr/bin/env python3
"""Retired: the legacy catalog reset did not preserve relational data or photos."""
import sys

if __name__ == '__main__':
    sys.exit('Catalog reset is retired and no data was changed. To start a separate empty install, use a new data directory. Preserve the old data and take a verified full backup first; see DEPLOYMENT.md.')
